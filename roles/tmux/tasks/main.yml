---
- hosts: all
  become: yes
  become_user: root
  become_method: sudo
  gather_facts: no
  tasks:
    - name: Install tmux
      apt:
        name: tmux
        state: present
        update_cache: yes
...
